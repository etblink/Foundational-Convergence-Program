# Program-Level Recurrence Delta Recomputation — Preregistration 0.1.0

**Operation:** `PROGRAM_LEVEL_RECURRENCE_DELTA_2026_09`
**Status:** PREREGISTERED BEFORE ADJUDICATION
**Date:** 2026-09-10
**Base canonical main:** `baeda13f9140d7e0d50adfef36546f26fae3f999`
**Method:** FCP Method 0.2.x recurrence rules as already canonically qualified; no new scoring rule is introduced here.
**New external scientific sources:** 0

## 1. Purpose

Recompute only the program-level recurrence consequences of the newly canonical Reduced-NFC ↔ `FW-PROCESS-MATRIX` prospective comparison and its already-canonical process-matrix antecedents. The operation is a delta recomputation against the previously qualified `PROGRAM_LEVEL_RECURRENCE_RECOMPUTATION_ADJUDICATION_0_1_0.md`, not a fresh unrestricted reinterpretation of the entire program.

The operation asks whether the new current pairwise slot changes any recurrence-family disposition, creates any independent non-generic foundational recurrence, creates any repeated independent support for Reduced NFC, or changes any program-level empirical-selection conclusion.

## 2. Frozen new delta inputs

The new scientific delta is limited to already-canonical repository artifacts at the base commit, principally:

- `audits/NFC_PROCESS_MATRIX_PROSPECTIVE_COMPARISON_ADJUDICATION_0_1_0.md`
- its preregistration, hostile false-positive audit, and handoff;
- `audits/FW_PROCESS_MATRIX_NULL_CONTROL_K9_TARGETED_PAIRWISE_REANALYSIS_ADJUDICATION_0_1_0.md` only where needed to resolve current process-matrix/null-control state;
- the already-canonical process-matrix admission and realizability/physical-selection records only where needed to prevent overclaim or double counting;
- the previous program-level recurrence recomputation and its frozen methodology artifacts.

No post-base source, commentary, framework paper, external audit, or new scientific source may alter the adjudication.

## 3. Frozen denominator rule

The new NFC/process-matrix prospective comparison is one new current effective pairwise slot because it compares a previously uncounted current pair under a separately preregistered prospective operation. It is not a replacement for the process-matrix/null-control slot and is not a second observation of any superseded NFC pair.

The expected arithmetic to test, not assume, is:

```text
PRIOR_CURRENT_EFFECTIVE_PAIRWISE_SLOT_COUNT = 13
PRIOR_CURRENT_EFFECTIVE_REDUCED_NFC_PAIRWISE_SLOT_COUNT = 6
DELTA_NEW_CURRENT_PAIRWISE_SLOT_COUNT = 1
DELTA_NEW_REDUCED_NFC_PAIRWISE_SLOT_COUNT = 1
CANDIDATE_NEW_CURRENT_EFFECTIVE_PAIRWISE_SLOT_COUNT = 14
CANDIDATE_NEW_CURRENT_EFFECTIVE_REDUCED_NFC_PAIRWISE_SLOT_COUNT = 7
```

If independent reconstruction contradicts these values, the reconstructed values control and the discrepancy must be recorded.

## 4. Frozen recurrence-family handling

The prior recurrence-family taxonomy R1–R10 remains unchanged. No new family class may be invented merely to accommodate the new slot.

The five positive relations reported by the new NFC/process-matrix comparison must be tested individually against the existing recurrence families rather than assumed to create five new families. A relation may increment support for an existing family without creating a new recurrence family.

The five reported positive relations to test are:

1. allowed-domain / carrier organization;
2. test-relative equivalence;
3. admissible-transformation organization;
4. operational-interface mediation;
5. global-coherence / globalization organization.

The following are explicitly barred from positive recurrence credit:

- shared absence of a unique dynamics/history law;
- shared absence of a scale law;
- shared incompleteness of physical realization or selection;
- shared lack of a framework-level empirical discriminator;
- vocabulary similarity without a source-qualified common functional role.

## 5. Strong-recurrence falsification burden

For every plausible R1 or R2 promotion, the operation must test all of:

- relation strength E1–E4 rather than E5-only;
- material non-genericity;
- incidence across distinct framework families;
- independence rather than common ancestry, reformulation, or target inheritance;
- absence of fixed-target import;
- source/provenance adequacy;
- explicit physical bridge where physical interpretation is claimed;
- calibration/EMP4 burden where empirical selection is claimed.

Failure of any required condition prevents R1/R2 promotion.

## 6. Reduced-NFC support rule

Generic E5 recurrence does not count as repeated independent support for Reduced NFC. Parallel burdens do not count as support. Comparator superiority or asymmetry does not transfer evidence to NFC. The new NFC/process-matrix slot may support Reduced NFC only if a non-generic, independently meaningful E1–E4 relation survives the frozen burdens.

## 7. Empirical firewall

No inherited empirical success, model-level constraint, realizability result, or process-matrix laboratory implementation is promoted to framework-level EMP4 without the already-required calibrated discriminator and decision rule. The delta operation cannot create empirical selection by reinterpretation alone.

## 8. Allowed outputs

The operation may create only:

1. this preregistration;
2. one delta recurrence adjudication artifact;
3. one qualification/handoff artifact;
4. bounded navigation/provenance reconciliation only after the scientific result is frozen, if required.

Historical scientific artifacts are immutable. No existing pairwise adjudication may be rewritten.

## 9. Required result fields

The adjudication must report at minimum:

```text
CURRENT_EFFECTIVE_PAIRWISE_SLOT_COUNT
CURRENT_EFFECTIVE_REDUCED_NFC_PAIRWISE_SLOT_COUNT
RECURRENCE_CANDIDATE_FAMILY_COUNT
R1 ... R10 counts
CURRENT_INDEPENDENT_FRAMEWORK_LEVEL_EMP4_SLOT_COUNT
CURRENT_MULTI_FAMILY_EMP4_RECURRENCE_COUNT
REDUCED_NFC_SUPPORTING_RECURRENCE_FAMILY_COUNT
HAS_REDUCED_NFC_ACCUMULATED_REPEATED_INDEPENDENT_SUPPORT
DOES_ANY_INDEPENDENTLY_NONGENERIC_COMMON_STRUCTURE_SURVIVE_ACROSS_MULTIPLE_DISTINCT_FRAMEWORK_FAMILIES
FRAMEWORK_WINNER
SCALAR_FRAMEWORK_SCORE
```

It must also distinguish `FAMILY_COUNT_CHANGED` from `FAMILY_SUPPORT_INCIDENCE_CHANGED`.

## 10. Stop conditions

Stop and report rather than force a conclusion if the canonical base is not exact, if the prior recurrence rules cannot be reconstructed, if the new pairwise slot cannot be distinguished from an existing/superseded slot, or if a material provenance conflict prevents exact current-state reconstruction.

## 11. Epistemic intent

A null delta is a successful scientific result. The purpose is not to manufacture novelty or convergence; it is to determine whether the new process-matrix information changes the program's recurrence claims under the same burden previously applied to other frameworks.